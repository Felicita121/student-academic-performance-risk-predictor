# StudentIQ backend

This directory contains the Flask API, shared feature-engineering and model
pipeline, trained Logistic Regression bundle, cross-validation report and API
tests. See the repository-level `README.md` for setup, role mapping, endpoint
documentation and deployment guidance.

Quick verification:

```bash
python ml_pipeline.py
python -m unittest -v test_api.py
python app.py
```
